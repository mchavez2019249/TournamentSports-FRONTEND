import { SearchLeaguePipe } from './search-league.pipe';

describe('SearchLeaguePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLeaguePipe();
    expect(pipe).toBeTruthy();
  });
});
